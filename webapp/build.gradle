task run(type: Exec) {
    executable "sh"
    args "-c", "npm install && (npm start &)"
}

task install(type: Exec) {
    executable "sh"
    args "-c", "npm install"
}

task build(type: Exec) {
    executable "sh"
    args "-c", "npm install && npm run pack"
}


